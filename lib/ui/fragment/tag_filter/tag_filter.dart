import 'package:flutter/material.dart';

class TagFilterFragment extends StatelessWidget {
  const TagFilterFragment({Key? key, required this.tags}) : super(key: key);
  final String tags;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
