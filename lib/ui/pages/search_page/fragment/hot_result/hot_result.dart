import 'package:flutter/material.dart';

class HotResult extends StatelessWidget {
  const HotResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
